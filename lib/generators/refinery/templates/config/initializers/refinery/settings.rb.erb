# encoding: utf-8
Refinery::Settings.configure do |config|
  # Configure whether to show settings in the admin menu, and enable admin users to change these settings
  # config.enable_interface = <%= Refinery::Settings.enable_interface.inspect %>
end